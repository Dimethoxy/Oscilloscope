# cmake-variants.yaml

unit_testing:
  default: debug
  choices:
    debug:
      short: Debug
      long: Debug build with debug information
      buildType: Debug
    release:
      short: Release
      long: Release build with optimization
      buildType: Release
